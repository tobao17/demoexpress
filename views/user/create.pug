extends ../../layout/common
block content 
    h1 Create user
    if error
        each err in error
          .alert.alert-danger=err
    form(action="/user/create",method="POST" enctype="multipart/form-data"  )        
        label(for="name") Tên người dùng
        input#name.form-control(type='text', name='name' placeholder='' value=values? values.name:'')
        label.mt-3(for="Phone") Số điện thoại
        input#Phone.form-control(type='text', name='Phone' placeholder='' value=values? values.Phone:'' )
        label.mt-3(for="Phone") avatar
        input#Phone.form-control(type='file', name='avatar' placeholder='' value=values? values.avatar:'' )
        button.btn.btn-primary.mt-3(type='submit') Create
        
